{% extends 'layout.html.twig' %}


{% block title %}Nuevo planeta{% endblock %}


{% block body %}
    <h1 class="text-center page-header">Crear nou Planeta</h1>

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_widget(form) }}
        </div>
        <div class="form-group">
            <input class="btn btn-warning" type="submit" value="Crear" />
        </div>
    {{ form_end(form) }}
    
    <a class="btn btn-block btn-info" href="{{ path('planeta_index') }}">Tornar a la llista</a>
    
{% endblock %}
