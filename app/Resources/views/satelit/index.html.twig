{% extends 'layout.html.twig' %}



{% block titulo %}Lista de satélites{% endblock %}


{% block body %}
    <h1>Satelits list</h1>

    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for satelit in satelits %}
            <tr>
                <td><a href="{{ path('satelit_show', { 'id': satelit.id }) }}">{{ satelit.nom }}</a></td>
                <td>{{ satelit.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('satelit_show', { 'id': satelit.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('satelit_edit', { 'id': satelit.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('satelit_new') }}">Create a new satelit</a>
        </li>
    </ul>
{% endblock %}
