{% extends 'layout.html.twig' %}



{% block title %}Nuevo satélite{% endblock %}


{% block body %}
    <h1>Crea un nou satèl·lit</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <div class="btn-group">
        <a class="btn btn-info" href="{{ path('satelit_index') }}">Back to the list</a>
    </div>
{% endblock %}
